#include <stdio.h>
#include "main.h"
/**
 * main - program to print all arg
 * @argc: no of arg
 * @argv: array
 *
 * Return: all args
 */
void main(int argc, char *argv[])
{
	int a;

	while (a < argc)
	{
		printf("%s\n", argv[i]);
		i++;
	}
	return (0);
}
