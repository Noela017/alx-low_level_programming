#include <stdio.h>
#include "main.h"
/**
 * progname - programme to output its name
 * @argc: num of command line arg
 * @argv: array
 * Return: 0 if success
 */
void progname(char *argv[])
{
	int a = 0;
	printf("%s\n", argv[a]);
}
